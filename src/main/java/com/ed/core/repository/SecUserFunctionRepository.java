package com.ed.core.repository;

import com.ed.core.entity.SecUserFunction;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SecUserFunctionRepository extends JpaRepository<SecUserFunction, Integer> {
}